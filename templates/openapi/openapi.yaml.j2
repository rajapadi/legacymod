openapi: 3.0.3
info:
  title: "{{ title }}"
  description: >-
    OpenAPI 3 draft generated by legacymod from unit {{ unit.name }}'s
    record layouts (COMMAREA/screen copybooks). Field types are
    PIC-derived; every schema property carries x-legacy-source pointing
    at the copybook item it came from. This is the API-enablement path
    for modernize-in-place dispositions - a draft for human refinement,
    not a finished contract.
  version: 0.1.0
paths:
{{ paths_yaml }}
components:
  schemas:
{{ schemas_yaml }}

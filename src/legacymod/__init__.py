"""legacymod: local-first, auditable mainframe-modernization platform.

Pipeline: ingest -> analyze -> graph -> assess -> docs -> rules ->
decompose -> spec -> datamig -> generate -> validate, plus operational
analytics (interfaces, runs, reconcile, flows) and reporting.

Architectural law: deterministic parsing and the knowledge graph are the
source of truth. LLM output is optional, always traceable, always marked
with origin/confidence/needs_review, and gated by human approval.
"""

__version__ = "0.1.0"
